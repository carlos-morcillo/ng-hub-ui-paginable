import { NbTableSorterExpandingRowDirective } from './nb-table-sorter-expanding-row.directive';

describe('NbTableSorterExpandingRowDirective', () => {
  it('should create an instance', () => {
    const directive = new NbTableSorterExpandingRowDirective();
    expect(directive).toBeTruthy();
  });
});
