import { TypeaheadOptionDirective } from './typeahead-option.directive';

describe('TypeaheadOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeaheadOptionDirective();
    expect(directive).toBeTruthy();
  });
});
