import { TypeaheadNoItemsDirective } from './typeahead-no-items.directive';

describe('TypeaheadNoItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeaheadNoItemsDirective();
    expect(directive).toBeTruthy();
  });
});
