import { TypeaheadPlaceholderDirective } from './typeahead-placeholder.directive';

describe('TypeaheadPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeaheadPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
