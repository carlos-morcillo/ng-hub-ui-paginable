import { TypeaheadHeaderDirective } from './typeahead-header.directive';

describe('TypeaheadHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeaheadHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
